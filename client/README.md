# Flutter client foundation

This directory is the planned Flutter application boundary for Android and Windows.

The first client milestone is intentionally small:

1. Start the application.
2. Load the configured API base URL.
3. Call the backend health endpoint.
4. Display whether the API is reachable.

Authentication, local encrypted storage, E2EE and file transfer will be added as separate milestones after this foundation is verified.
